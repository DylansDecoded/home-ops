---
# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/external-secrets.io/externalsecret_v1beta1.json
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: cross-seed
spec:
  refreshInterval: 5m
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: cross-seed-secret
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        config.js: |
          module.exports = {
            apiKey: "{{ .cross_seed_api_key }}",
            delay: 30,
            qbittorrentUrl: "http://qbittorrent.default.svc.cluster.local:8080",
            radarr: [
              "http://radarr.default.svc.cluster.local:7878/?apikey={{ .RADARR_API_KEY }}",
            ],
            sonarr: [
              "http://sonarr.default.svc.cluster.local:8989/?apikey={{ .SONARR_API_KEY }}",
            ],
            torznab: [
              "http://prowlarr.default.svc.cluster.local:9696/5/api?apikey={{ .PROWLARR_API_KEY }}",
              "http://prowlarr.default.svc.cluster.local:9696/6/api?apikey={{ .PROWLARR_API_KEY }}",
            ],

            action: "inject",
            duplicateCategories: true,
            flatLinking: false,
            includeSingleEpisodes: false,
            includeNonVideos: false,
            matchMode: "safe",

            linkCategory: "cross-seed",
            linkType: "hardlink",
            linkDir: "/data/media/Downloads/qbittorrent/complete/cross-seed",

            outputDir: "/qbittorrent/xseeds",
            torrentDir: "/qbittorrent/qBittorrent/BT_backup",
          }
  dataFrom:
    - extract:
        key: prowlarr
    - extract:
        key: radarr
    - extract:
        key: sonarr
    - extract:
        key: cross-seed